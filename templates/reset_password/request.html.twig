{% extends 'base.html.twig' %}

{% block title %}Réinitialiser votre mot de passe{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}
    <h1 class="flex_title title_content">Réinitialiser votre mot de passe</h1>

    <div class="flex_content reset_password_content">
        {{ form_start(requestForm) }}
            {{ form_row(requestForm.email) }}
            <div class="mb-15">
                <small>
                    Insérez votre adresse email et nous vous enverrons un lien pour modifier votre mot de passe.
                </small>
            </div>

            <div class="center-item"><button class="confirm_button">Envoyer le mail</button></div>
        {{ form_end(requestForm) }}
    </div>
    
{% endblock %}
